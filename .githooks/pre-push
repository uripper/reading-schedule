#!/usr/bin/env bash
set -euo pipefail

echo "[pre-push] Running pnpm run ci:local"
if pnpm run ci:local; then
  echo "[pre-push] All checks passed. Proceeding with push."
  exit 0
fi

echo "[pre-push] Checks failed. Push blocked."
exit 1
